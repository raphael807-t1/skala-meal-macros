# 수정 로그

간략하게 단계별로 기록. 새 변경사항은 맨 아래에 추가.

1. **프로젝트 시작** — 김현수님 서비스의 숨은 API(`/api/menus/current`) 발견, 로컬 sLLM(Ollama qwen3:8b)으로 영양정보 추정, `docs/index.html` 페이지 + Slack Webhook 전송 + 로컬 cron 자동화 구축
2. **GitHub Pages 배포** — 상세페이지를 `raphael807-t1.github.io/skala-meal-macros`로 공개 배포, cron 실행 시 자동 push되게 연결
3. **Slack 메시지 포맷 개선** — 중식/석식 각각 소계 표시, 자세히보기 링크 추가
4. **sLLM 정확도 문제 발견** — 칼로리를 실제보다 높게 잡는 경향 확인(FatSecret과 비교), sLLM 호출 비활성화(주석처리), GPT(gpt-4o-mini)로 대체
5. **식품안전나라 API(I0750) 연동 시도 → 실패** — 인증키에 우연히 `/` 문자가 포함되어 URL 경로 기반 인증 방식과 충돌(서버가 %2F는 404, literal `/`는 파라미터 위치를 밀어버림). 원인 규명 후 이 API 포기
6. **DB 우선순위 오케스트레이션 도입** — GPT를 "숫자 생성"이 아니라 "DB 검색어/구성요소 분석" 역할로 전환. Level1(원본검색)→Level2(유사어검색)→Level3(구성요소분해)→최후수단(GPT 직접추정) 구조로 개편, 최종 kcal는 항상 코드가 탄×4+단×4+지×9로 검증
7. **data.go.kr 식약처 API로 교체 → 성공** — `apis.data.go.kr/1471000/FoodNtrCpntDbInfo02` (쿼리스트링 인증이라 슬래시 문제 없음), 파라미터명 삽질 끝에 `FOOD_NM_KR`이 정답인 것 확인. 31만 건 실측 DB 정상 연동
8. **숫자 반올림 버그 수정** — 총합 계산 시 부동소수점 오차로 `21.400000000000002g` 같은 값이 나오던 것 수정, 전체 소수점 첫째자리로 통일
9. **DB 매칭 품질 개선 완료** — DB 검색 성공 = 무조건 정확이 아님을 인지하고 3가지 수정: (a) `food_heuristics.py` 신설, 카테고리별 예상 1회 제공량으로 100g 기준 DB값을 스케일링(쌀밥 166kcal/100g → 실제 200g로 332kcal), (b) `nutrition_db.lookup_best()`가 검색 후보 10개 중 문자열 유사도(difflib) 최고점을 채택하고 유사도를 결과에 포함, 유사도에 따라 신뢰도 등급(high/medium) 차등 부여, (c) 카테고리별 kcal 상한선 기반 이상치 탐지(⚠️ 표시만, 원본값 수정 안 함). GPT 호출 추가 없이 전부 코드 로직으로 처리(비용 절감)
10. **끼니 단위 이중계산 방지** — "장터국밥(밥 포함 완전식) + 쌀밥(별도 제공)"처럼 실제로는 밥이 중복 계산되는 반례 발견. `estimate_gpt.check_meal_duplicates()`를 끼니당 1번(하루 최대 2번, 저비용)만 불러서 판단. 첫 구현 때 GPT가 관련 없는 끼니에서도 "장터국밥" 언급하는 환각이 발견돼서, `contained_in`(근거 메뉴)이 실제로 같은 끼니 목록에 있는지 코드로 검증하는 방어 로직 추가 — 근거 불충분하면 자동 무시. 제외된 메뉴는 화면에서 안 지우고 흐리게 표시 + "🔁합계제외(중복)" 배지만 붙이고, 총합 계산에서만 제외
11. **`test_duplicate_detection.py` 추가, 5건 테스트로 GPT 판단 검증** — 실제 과거 메뉴 조합 5개(진짜 중복/함정케이스/애매한 케이스 섞음)로 테스트한 결과 5건 중 2건 오탐("소고기쌀국수"를 밥 포함으로 착각, "닭곰탕"도 잘못 제외) 발견. 사용자 피드백대로 "밥이 포함될 수 있는 건 국밥·비빔밥뿐"이라는 화이트리스트를 코드 레벨에 강제 게이트로 추가(`contained_in`이 "국밥"/"비빔밥"으로 안 끝나면 무조건 기각) → 재테스트 5/5 통과. 프롬프트에 있던 구체적 예시("장터국밥")도 편향 방지를 위해 일반화
12. **중복제외 버그 수정: 다른 끼니 동일메뉴까지 잘못 제외되던 문제** — 배포 후 실제 페이지에서 "중식 쌀밥"까지 흐리게 제외 표시되는 걸 발견(중식엔 국밥류가 없었는데도). 원인: `macros` 딕셔너리가 메뉴명을 키로 써서 점심 "쌀밥"과 저녁 "쌀밥"이 완전히 같은 객체를 공유 → 저녁에서만 제외 판단이 나도 그 객체에 플래그를 직접 박아버려서 점심 것도 같이 바뀜. `macros`를 직접 수정하지 않고, 끼니별로 분리된 `{"lunch": {...}, "dinner": {...}}` 형태의 exclude set을 따로 만들어 넘기는 구조로 수정
13. **UI 다듬기** — 출처 뱃지 이모지(🟢🟡🟠) 복구, 제목/중식·석식 부제목에도 이모지 추가(🍽️/🥗/🍲), 중식·석식 표 컬럼 너비를 `colgroup`으로 통일해서 줄 정렬 맞춤, 탄:단:지 비율 열 삭제. 제외된 메뉴(예: 중복 쌀밥)는 흐리게만 하지 않고 값에 취소선을 긋고 "🔁 장터국밥에 포함"처럼 이유를 바로 옆에 텍스트로 표시하도록 `excluded_by_meal`을 set에서 `{메뉴명: 포함된메뉴명}` dict로 변경(Slack 메시지에도 동일하게 반영)
14. **자동화 신뢰성 개선 (네트워크 대기, push 재시도)** — (a) `_push_page_to_github()`에 재시도 로직 추가(4회, 15초 간격) — pmset 웨이크 직후 Wi-Fi가 아직 안 붙어 push가 한 번 실패했던 사례(2026-08-24) 대응. (b) `_wait_for_network()` 신설 — 첫 네트워크 호출 전 DNS 해석을 최대 3분간 폴링, 2026-08-25에 `NameResolutionError`로 스크립트 전체가 재시도 없이 죽어서 그날 알림이 아예 안 갔던 사고 재발 방지
15. **"자세히보기" 링크가 항상 하루 전 날짜로 뜨던 문제 해결** — 매일 로컬 페이지는 정상 생성되는데 배포된 GitHub Pages만 하루 늦게 보여서 진짜 원인 추적. 원인: cron은 GUI 로그인 세션이 아니라서 git의 osxkeychain credential helper가 macOS 키체인에 접근 못 함 → push가 매번 `could not read Username for 'https://github.com': Device not configured`로 조용히 실패 → 로컬 커밋만 쌓이고 실제 배포는 사용자가 나중에 우연히 키체인이 열려있는 타이밍에야 반영돼서 "항상 하루 지연"처럼 보였음(재시도 횟수를 늘려도 인증 자체가 안 되니 무의미했음). 고침: `.env`에 `GITHUB_TOKEN`(fine-grained PAT, Contents: Read/write) 추가, `_push_page_to_github()`가 keychain 대신 push 시점에만 URL에 토큰을 실어 인증하도록 변경(토큰은 `.git/config`에는 남지 않음). 밀려있던 커밋 2개(8/26, 8/27) 수동 push로 즉시 반영 확인
